<% extend('Default/Default') %>

<div class="container salto">

    <div class="salto">
        <h5 class="textoGris"></h5>
        <p>Escanear con <span class="textMorado">Streetcred Identity Wallet</span> el siguiente codigo QR:</p>
        <img id='qrCode' src="https://chart.googleapis.com/chart?cht=qr&chl=<%= url %>&chs=300x300" />
    </div>
</div>

<script>
    document.addEventListener('DOMContentLoaded', (event) => {
        const ioClient = io.connect("http://37.135.11.37:3001");
        ioClient.on("<%= id %>", (token) => {
            location.replace("/app/"+token)
        });
    });

    /*var a = window.location.pathname.match("^\/app\/(.*)")
    if(a!=null && a.length==2){
        
    }*/

</script>